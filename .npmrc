# The runtime libraries are peer dependencies the host site supplies. The
# devDependencies mirror them only so `tsc --noEmit` can run here; keep npm
# from auto-installing any peer beyond that list (tailwindcss, for one).
legacy-peer-deps=true
