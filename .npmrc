# Runtime libraries are peer dependencies supplied by the host site. Keep npm
# from auto-installing them here: a node_modules inside this package would
# shadow the host's copies and give the calendar a second React, toast store
# and Auth0 context.
legacy-peer-deps=true
